<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tepak.carter.recycly">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:configChanges="layoutDirection|locale"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LangSelect"
            android:configChanges="layoutDirection|locale"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:configChanges="layoutDirection|locale"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".Pmdrecycle"
            android:configChanges="layoutDirection|locale" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PMDdoortodoor"
            android:configChanges="layoutDirection|locale" />
        <activity
            android:name=".Glassrecycle"
            android:configChanges="layoutDirection|locale" />
        <activity
            android:name=".MapsActivity_glass"
            android:configChanges="layoutDirection|locale"
            android:label="@string/title_activity_maps_paper" />
        <activity
            android:name=".Paperrecycle"
            android:configChanges="layoutDirection|locale" />
        <activity
            android:name=".Paperdoortodoor"
            android:configChanges="layoutDirection|locale" />
        <activity
            android:name=".PMD_Upcycle"
            android:configChanges="layoutDirection|locale" />
        <activity
            android:name=".Settings"
            android:label="My new title"
            android:configChanges="layoutDirection|locale" />
        <activity android:name=".Other_recycle" />
        <activity android:name=".Glass_Upcycle" />
        <activity android:name=".Paper_Upcycle"></activity>
    </application>

</manifest>